inherit gnome2

DESCRIPTION="GNOME web browser"

PATCH_URI="2.27-implib.patch"

PKG_NAMES="${PN} ${PN}-devel"
epiphany_CONTENTS="--exclude=aclocal --exclude=gtk-doc etc/ usr/bin/ usr/share/"
epiphany_devel_CONTENTS="usr/include/ usr/lib/ usr/share/aclocal/
                         usr/share/gtk-doc/"

CYGCONF_ARGS="--disable-network-manager --disable-zeroconf"
